<!doctype html>
<meta charset=utf-8>
<title>{{.StatusCode}} {{httpStatusText .StatusCode}} – Spork</title>
<link rel=stylesheet href="{{assetPath "/style.css"}}">
<h1>{{.StatusCode}} {{httpStatusText .StatusCode}}</h1>
<p>{{with .Type}}{{.}}: {{end}}{{with .Name}} {{.}}: {{end -}} {{.Message}}</p>